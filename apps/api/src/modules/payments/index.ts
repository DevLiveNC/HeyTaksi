/** payments alanının Faz 2 servis giriş noktası. İş kuralları bu dizinde route/service/repository olarak ayrılacaktır. */
export const paymentsModule = { name: 'payments', status: 'reserved' } as const;
