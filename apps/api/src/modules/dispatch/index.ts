/** dispatch alanının Faz 2 servis giriş noktası. İş kuralları bu dizinde route/service/repository olarak ayrılacaktır. */
export const dispatchModule = { name: 'dispatch', status: 'reserved' } as const;
